version: "3.9"
services:
  frontend:
      build:
        context: .
        dockerfile: Dockerfile
      container_name: salesClient
      working_dir: /app
      env_file: ".env"
      volumes:
         - '.:/app'
         - /app/node_modules/ 
      ports:
        - "5173:5173"